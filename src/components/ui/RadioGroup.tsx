export function RadioGroup({ value, options, onChange }: { value: string; options: readonly { value: string; label: string; description?: string; disabled?: boolean }[]; onChange: (value: string) => void }) {
  return <div className="grid gap-px border border-[var(--border)] bg-[var(--border)] sm:grid-cols-2">{options.map((option) => <button key={option.value} type="button" disabled={option.disabled} onClick={() => onChange(option.value)} className={`min-h-16 px-3 py-3 text-left transition disabled:cursor-not-allowed disabled:opacity-45 ${value === option.value ? 'bg-[var(--surface-raised)] text-[var(--text)]' : 'bg-[var(--surface)] text-[var(--text-muted)]'}`}><span className={`mr-2 inline-block ${value === option.value ? 'text-[var(--primary)]' : 'text-[var(--text-muted)]'}`}>{value === option.value ? '◆' : '◇'}</span><span className="font-hud text-sm font-semibold">{option.label}</span>{option.description && <span className="mt-1 block pl-5 text-xs leading-5">{option.description}</span>}</button>)}</div>;
}
